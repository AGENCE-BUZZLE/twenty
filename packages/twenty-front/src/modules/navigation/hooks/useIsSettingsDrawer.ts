// Buzzle: settings pages keep the main workspace drawer (dark Ink with
// Buzzle nav) instead of swapping to Twenty's settings sidebar. Our
// settings surface is intentionally minimal (Profil + Membres) with its
// own inline sub-nav rendered inside the page content.
export const useIsSettingsDrawer = () => false;
